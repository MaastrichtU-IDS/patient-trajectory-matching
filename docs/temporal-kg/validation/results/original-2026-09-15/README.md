# Original evidence, 15 September 2026

`reasoner-checks.txt`, `owl2dl-profile.txt` and `document-checks.txt` are unchanged copies retained from the v2 report-authoring workspace. Their hashes are in `../../provenance.json`.

`checks.tsv` is a reconstruction added for issue #6, not an original machine output. Expected and actual Booleans are traced to successful assertions in `../../ValidateTemporal.java` and the indicated original transcript line. Separate original stdout/stderr for compilation/runtime versions and timestamped command records were not retained. The separately identified rerun captures those outputs.

See `../../README.md` for report correspondence, versions and reproduction.
