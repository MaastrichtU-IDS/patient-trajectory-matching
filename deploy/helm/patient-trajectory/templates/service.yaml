apiVersion: v1
kind: Service
metadata:
  name: {{ include "trajectory.name" . }}
  labels:
    {{- include "trajectory.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "trajectory.labels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
